# This repo tracks only the Chinese Classical scoring worksheet.
# Ignore everything else in the surrounding folder by default.
/*

# ...but keep these tracked files.
!.gitignore
!LICENSE
!chinese_classical_scoring_worksheet_dynamic.html
